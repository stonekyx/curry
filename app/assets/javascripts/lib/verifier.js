(function($){
  /**
   * @namespace Verifier Lib
   */
  Curry.Verifier = {
    /*
     * Whether current user is a anonymous user.
     * @return {boolean} whether is a anonymous user.
     */
    isAnonymousUser: function() {
      return true;
    },
  }
}).call(this, jQuery);
